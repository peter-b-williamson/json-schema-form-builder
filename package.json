{
  "name": "json-schema-form-builder",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1"
  },
  "lint-staged": {
    "form-builder/**/*.{ts,vue,js}": [
      "npm --prefix form-builder run lint"
    ],
    "form-builder/**/*.{ts,vue,js,json,css,md}": [
      "npm --prefix form-builder run format"
    ]
  }
}
