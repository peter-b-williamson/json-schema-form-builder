{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Generated by json-schema-form-builder on 2026-08-01T12:00:00.000Z",
  "type": "object",
  "properties": {
    "fullName": { "title": "Full name", "type": "string", "minLength": 2 },
    "country": { "title": "Country", "enum": ["uk", "us"] }
  },
  "required": ["fullName"],
  "allOf": [
    {
      "if": {
        "properties": { "country": { "enum": ["us"] } },
        "required": ["country"]
      },
      "then": {
        "properties": { "state": { "title": "State", "type": "string" } }
      }
    }
  ]
}
